## 暖身題:`std::sort` + vector

**建一個 `std::vector<int>` 裝 `{5, 2, 8, 1, 9}`,用 `std::sort` 排序,再用 range-based for 印出來。**

要點:
- 記得 `#include <vector>` 和 `#include <algorithm>`
- 排序一行:`std::sort(v.begin(), v.end());`
- 印出來用 `for (int x : v)`

